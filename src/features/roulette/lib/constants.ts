/**
 * ルーレット中央から見せる前後の候補位置です。
 */
export const visibleOffsets = [-2, -1, 0, 1, 2];

/**
 * ルーレットの見た目を更新する間隔です。
 */
export const spinIntervalMs = 90;

/**
 * ルーレット演出を継続する時間です。
 */
export const spinDurationMs = 2600;
